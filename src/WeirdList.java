public class WeirdList {
    private INode<NodeMain> firstNode;

    public WeirdList(INode<NodeMain> firstNode) {
        this.firstNode = firstNode;
    }

    public INode<NodeMain> getFirstNode() {
        return firstNode;
    }
}
